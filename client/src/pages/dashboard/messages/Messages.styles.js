import styled from 'styled-components';

const Messages = styled.main``;
const Header = styled.header``;
const NewMessage = {
  Main: styled.div``,
  Header: styled.div``,
  Content: styled.div``,
};

export { Messages, Header, NewMessage };
