import styled from 'styled-components';

const PostForm = styled.form``;
const FileContainer = styled.div``;
const File = styled.img``;
const Error = styled.div``;
export { PostForm, FileContainer, File, Error };
