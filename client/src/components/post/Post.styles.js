import styled from 'styled-components';

const Post = styled.div``;
const Settings = styled.div``;
const Header = styled.header``;
const Content = styled.div``;
const Buttons = styled.div``;

export { Post, Settings, Header, Content, Buttons };
